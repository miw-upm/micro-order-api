services:
  order-api-server:
    build: .
    image: order-api:v1
    container_name: order-api-server
    restart: always
    ports:
      - "8082:8082"
    networks:
      - miw

networks:
  miw:
    external: true

#> docker compose up --build -d